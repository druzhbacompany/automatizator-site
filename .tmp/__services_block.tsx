      <Section id="services" title="Услуги" lead="Делаем сложное простым: подключаем то, что даёт результат уже на этой неделе. Понятные сроки, фикс-прайс на стартовый пакет, прозрачные метрики.">
        {services.map((s) => (
          <ServiceCard key={s.id} s={s} />
        ))}
      </Section>
